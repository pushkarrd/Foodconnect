# 📊 Collections Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      FoodConnect App                             │
│                   (React + Vite + Tailwind)                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
          ┌──────────────┴──────────────┐
          │                             │
    ┌─────▼──────┐            ┌────────▼────────┐
    │   Pages    │            │   Components    │
    │ (9 Files)  │            │   (4 Files)     │
    └─────┬──────┘            └────────┬────────┘
          │                            │
          └──────────────┬─────────────┘
                         │
                  ┌──────▼──────┐
                  │  Services   │
                  │  (3 Files)  │
                  └──────┬──────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        │                │                │
   ┌────▼────┐    ┌──────▼─────┐    ┌────▼────────┐
   │ Firebase │    │  Firebase  │    │   Firebase  │
   │   Auth   │    │ Firestore  │    │   Storage   │
   │          │    │            │    │             │
   └────┬─────┘    └──────┬─────┘    └─────────────┘
        │                 │
        │            ┌────┴─────────┐
        │            │              │
   ┌────▼────────────▼──┐    ┌──────▼──────┐
   │   Create User       │    │ Collections │
   │   Login/Logout      │    │ - users     │
   │   Profile Data      │    │ - donations │
   │                     │    │ - bookings  │
   └─────────────────────┘    └─────────────┘
```

---

## Data Flow Diagram

### User Signup Flow
```
┌──────────────┐
│  Signup Page │
└──────┬───────┘
       │ (name, email, password, phone, address, role, location)
       ▼
┌──────────────────────┐
│ registerUser()       │ (authService.js)
│ in authService.js    │
└──────┬───────────────┘
       │
       ├─► Create Firebase Auth User
       │
       └─► Create 'users' collection document
           {
             uid, name, email, phone,
             address, role, location,
             ratings: [], averageRating: 0
           }
       │
       ▼
┌──────────────┐
│ Dashboard    │ (Donor or Receiver based on role)
└──────────────┘
```

---

### Create Donation Flow
```
┌────────────────────┐
│ Add Donation Page  │
└──────┬─────────────┘
       │ (foodType, quantity, description, image, location, pickupTime)
       ▼
┌──────────────────────┐
│ createDonation()     │ (donationService.js)
└──────┬───────────────┘
       │
       ├─► Upload image to Firebase Storage
       │   → Get image URL
       │
       └─► Create 'donations' collection document
           {
             donorId, foodType, quantity,
             description, imageURL,
             location, address,
             status: "Pending",
             createdAt, expiresAt
           }
       │
       ▼
┌──────────────────────┐
│ Show in Donor        │
│ Dashboard            │
│ &                    │
│ Push to Receiver     │
│ Real-Time List       │
└──────────────────────┘
```

---

### Search & Display Flow
```
┌──────────────────────┐
│ Receiver Dashboard   │
└──────┬───────────────┘
       │ (Get receiver location)
       ▼
┌──────────────────────────────┐
│ subscribeToNearbyDonations() │ (donationService.js)
│ Real-time listener           │
└──────┬───────────────────────┘
       │
       ├─► Watch 'donations' collection
       │
       ├─► Filter: status = "Pending"
       │
       ├─► For each donation:
       │   - Calculate distance using Haversine
       │   - Keep if distance <= 15 km
       │   - Sort by distance (nearest first)
       │
       └─► Callback returns updated list
           Automatically re-runs when:
           - New donation added
           - Donation status changes
           - Donation deleted
       │
       ▼
┌──────────────────────┐
│ Display in List      │
│ (Real-time updates)  │
└──────────────────────┘
```

---

### Booking Flow
```
┌──────────────────────┐
│ Donation Detail Page │
└──────┬───────────────┘
       │ Click "Book This Food"
       ▼
┌──────────────────────┐
│ bookDonation()       │ (donationService.js)
└──────┬───────────────┘
       │
       ├─► Update 'donations' doc:
       │   - status: "Pending" → "Booked"
       │   - receiverId: null → receiverUID
       │   - bookedAt: timestamp
       │
       └─► Create 'bookings' document:
           {
             donationId,
             receiverId,
             status: "Active",
             bookedAt: timestamp
           }
       │
       ▼
┌──────────────────────────────┐
│ Donation removed from other  │
│ receivers' lists (real-time) │
│                              │
│ Show in receiver's           │
│ "My Bookings" page           │
└──────────────────────────────┘
```

---

## Collection Relationships

```
┌──────────────┐
│ users        │ (uid as doc ID)
├──────────────┤
│ uid          │
│ name         │ Donor or Receiver
│ email        │ profile created
│ phone        │ at signup
│ role         │
│ address      │
│ location     │
│ ratings[]    │
└──────┬───────┘
       │
       │ 1 user can create many donations
       │ (via donorId field)
       │
       ▼
┌──────────────────────────────────┐
│ donations                        │
├──────────────────────────────────┤
│ donorId ────────┐                │
│ foodType        │                │
│ quantity        ├─→ Links to user│
│ description     │  in users      │
│ imageURL        │  collection    │
│ status          │                │
│ receiverId      │ (null or uid)  │
│ location        │                │
│ createdAt       │                │
└──────┬──────────────────────────┘
       │
       │ 1 donation can have many bookings
       │ (via donationId field)
       │
       ▼
┌──────────────────────┐
│ bookings             │
├──────────────────────┤
│ donationId ──────┐   │
│ receiverId       ├──→ Links to both
│ status           │   donations and
│ bookedAt         │   users collections
└──────────────────┘
```

---

## Real-Time Updates Architecture

```
┌─────────────────────────────────────┐
│ Receiver's Browser                  │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ subscribeToNearbyDonations()    │ │
│ │ Sets up real-time listener      │ │
│ └──────────────┬──────────────────┘ │
│                │                     │
└────────┬───────┴─────────────────────┘
         │
         │ WebSocket connection
         │ (Firebase real-time)
         │
         ▼
┌────────────────────────────────────┐
│ Firestore Database                 │
│                                    │
│ ┌──────────────────────────────┐  │
│ │ donations collection         │  │
│ │                              │  │
│ │ Listener watches for:        │  │
│ │ - New documents added        │  │
│ │ - Existing docs updated      │  │
│ │ - Docs deleted               │  │
│ └──────────────────────────────┘  │
└────────────────────────────────────┘
         │
         │ Any change → triggers callback
         │
         ▼
┌─────────────────────────────────────┐
│ Receiver's Browser Updated          │
│                                     │
│ Shows new donations instantly       │
│ (No page refresh needed!)           │
│                                     │
│ Real-time updates ⚡                │
└─────────────────────────────────────┘
```

---

## Authentication & Role-Based Access

```
┌────────────────────────────────┐
│ User Signs Up                  │
└─────┬──────────────────────────┘
      │
      ▼
┌────────────────────────────────┐
│ Select Role                    │
├────────────────────────────────┤
│ ├─ I'm a Donor (Share Food)    │
│ └─ I'm a Receiver (Need Food)  │
└─────┬──────────────────────────┘
      │
      ├─────────────┬─────────────┐
      │             │             │
      ▼             ▼             ▼
   ┌──────┐    ┌──────────┐   ┌──────────┐
   │ User │    │ Stored   │   │ Router   │
   │ Doc  │    │ in users │   │ Checks   │
   │ Role │    │ (role    │   │ and      │
   │      │    │ field)   │   │ Redirects│
   └──┬───┘    └──────────┘   └──────┬───┘
      │                             │
      │                      ┌──────┴──────┐
      │                      │             │
      ▼                      ▼             ▼
   ┌──────────────────┐  ┌────────┐  ┌──────────┐
   │ /donor/dashboard │  │ Donor  │  │ Receiver │
   │ /donor/add-...   │  │ Pages  │  │ Pages    │
   │ /donor/profile   │  │        │  │          │
   └──────────────────┘  └────────┘  └──────────┘
```

---

## Field Dependency Map

```
User Signup
    │
    └─► users.uid ────────────┐
        users.role            │
        users.location        │
                              │
                    ┌─────────┴────────┐
                    │                  │
                    ▼                  ▼
            Donor creates        Receiver searches
            donations            donations
                    │                  │
                    └─► donations.donorId
                        donations.location ◄── uses users.location
                        donations.status      for distance calc
                        donations.imageURL
                              │
                              ▼
                        Receiver books
                        donation
                              │
                    ┌─────────┴────────┐
                    │                  │
                    ▼                  ▼
            donations.receiverId  bookings.donationId
            donations.status      bookings.receiverId
            donations.bookedAt    bookings.status
```

---

## Collection Size Estimation

```
For 1000 daily active users:

users collection:
├─ 500 Donors
└─ 500 Receivers
   = 500 documents

donations collection:
├─ Each Donor posts 1 donation/day
├─ 80% expire (not booked)
├─ 20% get booked
   = 500 active documents (varies)

bookings collection:
├─ 20% of 500 donations get booked
├─ Some users book multiple
└─ Average 3 bookings/day
   = ~200 documents (growing)
```

---

## Query Patterns

```
1. Get user profile:
   WHERE uid == currentUserUid
   
2. Get donor's donations:
   WHERE donorId == donorUid
   ORDER BY createdAt DESC
   
3. Get nearby pending donations:
   WHERE status == "Pending"
   ORDER BY createdAt DESC
   → Filter by distance in app
   
4. Get receiver's bookings:
   WHERE receiverId == receiverUid
   ORDER BY bookedAt DESC
   
5. Get specific donation:
   WHERE donationId == donationUid
```

---

## Security Flow

```
┌────────────────┐
│ Request to DB  │
└────┬───────────┘
     │
     ▼
┌────────────────────────────────┐
│ Firestore Security Rules       │
├────────────────────────────────┤
│ 1. Is user authenticated?      │
│    → Check request.auth        │
│                                │
│ 2. Has permission?             │
│    → Check request.auth.uid    │
│    → Compare with document     │
│                                │
│ 3. Collection rules:           │
│    - users: own data only      │
│    - donations: read all,      │
│      write own only            │
│    - bookings: read all,       │
│      create own only           │
└────┬───────────────────────────┘
     │
     ├─► ALLOW ──► Return data
     │
     └─► DENY ──► Throw error
```

---

## Complete Workflow Diagram

```
Step 1: SIGNUP
┌─────────────────┐
│ Donor Signs Up  │ → User created in 'users' ✓
└─────────────────┘

Step 2: CREATE DONATION
┌─────────────────────┐
│ Donor Uploads Food  │ → Image to Storage ✓
│                     │ → Document in 'donations' ✓
└─────────────────────┘

Step 3: RECEIVER SIGNS UP
┌──────────────────────┐
│ Receiver Signs Up    │ → User created in 'users' ✓
└──────────────────────┘

Step 4: SEARCH
┌─────────────────────────────┐
│ Receiver Opens Dashboard    │ → Real-time watch on 'donations' ✓
│ (25km away)                 │ → Distance calculated ✓
│                             │ → Shows "15km away" ✓
└─────────────────────────────┘

Step 5: BOOK
┌──────────────────────┐
│ Receiver Books Food  │ → Update 'donations' status ✓
│                      │ → Create 'bookings' doc ✓
└──────────────────────┘

Step 6: COLLECT
┌──────────────────────┐
│ Receiver Collects    │ → Update 'donations' status ✓
│                      │ → Update 'bookings' status ✓
└──────────────────────┘
```

---

**All collections integrated into your framework! 🚀**

*Turn excess food into community support* 🍱❤️
